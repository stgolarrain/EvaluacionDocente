<%= simple_form_for @academic_unit, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                academic_units_path, :class => 'btn' %>
  </div>
<% end %>
